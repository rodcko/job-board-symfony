<html>
<body>
    <table>
        <thead>
            <tr>
                <th>
                    Company
                </th>
                <th>
                    Title
                </th>
                <th>
                    &nbsp;
                </th>
            </tr>
        </thead>
        <tbody>
        {% for offer in offers %}
            <tr>
                <td>{{ offer.compnay }}</td>
                <td>{{ offer.title }}</td>
                <td><a href="/offer/apply?offerId={{ offer.id }}">Apply</a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</body>
</html>
